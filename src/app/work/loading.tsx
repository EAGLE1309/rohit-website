import MaxWidthWrapper from "@/components/layout/max-width-wrapper";

const WorkLoading = () => {
  return <MaxWidthWrapper className="h-[100vh] w-full overflow-hidden flex items-center justify-center gap-3">Loading...</MaxWidthWrapper>;
};

export default WorkLoading;
